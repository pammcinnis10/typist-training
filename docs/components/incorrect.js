import React from "react";

export default function Incorrect({}) {
  const resultStyle = {
    marginBottom: "0px",
    marginLeft: "20px",
    paddingLeft: "5px",
    paddingRight: "5px",
    fontWeight: "bold",
    borderRadius: "5px",
  };

  return <p style={resultStyle}>Try Again</p>;
}
